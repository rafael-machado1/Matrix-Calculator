#pragma once

// Header para soma de duas matrizes
// Chamando na main seria (exemplo de 3x3): add(3, a, b, sum)
// É necessário declarar sum para que ele retorne após o fim da função
void add(int tamanho, int a[tamanho][tamanho], int b[tamanho][tamanho], int sum[tamanho][tamanho]);